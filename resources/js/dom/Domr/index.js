import Component from './Component';
import ActiveComponent from './ActiveComponent';
import Router from './Router';

const Domr = {
  Component,
  ActiveComponent,
  Router,
};

export default Domr;
